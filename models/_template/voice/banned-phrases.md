# Per-model banned phrases (adds to prompts/slop-blocklist.txt)
# One per line. Plain text = case-insensitive substring. "re:" prefix = regex.
# "allow: <phrase>" whitelists a GLOBAL rule for this model (e.g. she genuinely says "obsessed").

# examples — replace with hers
# so cute
# re:\bgirls? night\b
# allow: obsessed
